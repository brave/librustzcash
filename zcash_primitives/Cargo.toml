[package]
name = "zcash_primitives"
description = "Rust implementations of the Zcash primitives"
version = "0.29.0"
authors = [
    "Jack Grigg <jack@z.cash>",
    "Kris Nuttycombe <kris@electriccoin.co>"
]
homepage = "https://github.com/zcash/librustzcash"
readme = "README.md"
edition = "2021"
license = "MPL-2.0"

# Brave: this crate is trimmed to only the `merkle_tree` and `transaction`
# (Orchard bundle + amount) surface consumed by brave-core's Orchard wallet
# code. Sapling, transparent, proofs and the rest of upstream zcash_primitives
# are removed to avoid pulling in their dependency trees.
[dependencies]
orchard = { version = "0.15.0", default-features = false }
incrementalmerkletree = { version = "0.8.2", features = ["legacy-api"] }
zcash_protocol = { version = "0.10.0", default-features = false }
zcash_encoding = { version = "0.4", default-features = false }
zcash_note_encryption = { version = "0.4.1", features = ["pre-zip-212"] }
memuse = "0.2.2"
nonempty = "0.11"
corez = { version = "0.1.1", default-features = false, features = ["alloc"] }
blake2b_simd = { version = "1", default-features = false }

[lib]
name = "zcash_primitives"
path = "src/lib.rs"
crate-type = ["rlib"]

# Brave: no-op marker feature, declared only so other workspace members'
# (e.g. `pczt`) feature requirement on this crate still resolves; the
# trimmed crate has no `zip-233`-gated code.
[features]
zip-233 = []
